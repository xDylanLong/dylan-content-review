---
name: dylan-content
description: Use when 用户要把抖音或小红书的选题、标题、口播稿或文案加工、诊断、复盘时。
---

# dylan-content：独立内容工作台

## 核心契约

这是一个独立、可持续修改的个人内容 skill。六个内容能力已经完整迁移到本目录的 `modules/` 中；执行时只读取本目录，不调用、加载或依赖已经删除的外部 skill 目录。

本 skill 覆盖：

- 选题到内容形式和表达方案；
- 小红书标题公式匹配；
- 短视频开头诊断；
- 逐字稿逻辑延续、信息密度和口播流畅度；
- 受众共鸣、立场和传播入口诊断；
- AI 写作特征识别，默认只诊断不改写；当被口播生成 Skill 调用时，额外作为生成后的表达质检。

## 模块选择

根据用户材料按需读取对应模块。需要组合时，按下列顺序执行：

| 用户任务 | 读取模块 |
|---|---|
| 选题、形式、内容方向、完整文稿诊断 | `modules/01-content-diagnosis.md` |
| 短视频开头、前 5 秒、开场流失 | `modules/02-hook-optimization.md` |
| 小红书标题、标题公式、标题改写 | `modules/03-xhs-title-formulas.md` |
| 逐字稿衔接、啰嗦、信息密度、划走位置 | `modules/04-script-flow.md` |
| 共鸣、受众情绪、立场、传播机制 | `modules/05-resonance-diagnosis.md` |
| AI 味、机器化表达、文案特征检查 | `modules/06-ai-expression-check.md` |

用户同时提供完整短视频稿时，默认读取 `01`、`02`、`04`、`05`、`06`；需要小红书版本时再加入 `03`。

## 统一工作方式

1. 先判断用户要解决的是选题、标题、开头、正文结构、共鸣还是表达特征问题。
2. 只读取当前任务所需的模块，保留模块里的完整诊断规则、输出格式和边界。
3. 先指出内容本身的问题，再给标题或开头优化；不要用技巧掩盖素材、观点或产品价值不足。
4. 以用户提供的原句、事实和素材为证据，不凭空补经历、数据、成绩或案例。
5. 结论要落到具体删改位置、替代句、内容骨架或下一步动作。
6. 不承诺完播率、点击率、推荐量或“必火”；只能报告结构风险和可验证改法。

## Dylan 统一输出

除非用户明确只要单项结果，否则将各模块结论整理为：

```markdown
# Dylan 内容工作台结果

## 核心判断
- 当前任务：
- 唯一核心机制：
- 最大问题：

## 内容方案
- 推荐平台与形式：
- 标题/封面方向：
- 短视频开头：
- 正文骨架或逐字稿修改：

## 风险与待补素材
- 兑现风险：
- 共鸣风险：
- AI/机器化表达：
- 需要补充的事实或案例：

## 下一步
一个今天可以完成的具体动作。
```

只要用户明确说“只改标题”“只检查开头”或“只看 AI 味”，就只输出对应模块的结果，不强行生成完整报告。

## 内容分层与自然转折

当一篇内容同时包含观众已有的常规判断和作者要补充的新判断时，必须把两层分开处理：

- 先讲已有观点：说明观众通常会怎么判断，以及这个判断在哪些条件下有用；
- 再让新判断出现：用案例结果、对比、异常代价或追问，让观众发现前面的判断还不够；
- 最后说出新增判断：可以直接说“这里要补一个判断”，但不要在口播中宣布“反直觉观点”“反常识观点”“容易被忽略的信号”“现在进入反转”等结构标签。

口播顺序固定为“常规判断 → 案例卡住 → 追问或对比 → 新判断”。两层之间至少有一个独立转折句和一个新段落，不能把已有观点、新观点和结论揉成一段。转折句要来自内容关系，例如“但把这个标准放回刚才的场景”“按这个标准看，两个结果不一样”“这时还要追问一个问题”，不要用模板提示观众“下面我要讲反常识”。

策划、诊断和脚本检查可以使用“已有观点层”“新增判断层”作为分析标签；这些标签不应直接进入录音稿。若新观点与已有观点重复，间隔一个案例、结果或推演后再出现，并更换证据或判断角度。

## 口播生成时的 AI 表达硬约束

当另一个内容 Skill 调用本 Skill 参与生成口播稿时，`modules/06-ai-expression-check.md` 不只用于发现问题，还要在交付前逐句做反向质检。以下规则优先于“看起来完整”和“分点齐全”：

- 一篇只保留一个核心判断。正文最多使用一组明确分点，一般 2–3 个就够；课程型讲解为了拆清判断可以到 3–4 个，但不能都用相同长度、相同语法和相同结尾。
- 不为了显得有结构，连续写“第一、第二、第三”，也不把四个抽象名词并排塞进一句话。能用一个具体场景讲清楚，就删掉列表。
- 默认不使用比喻。只有用户明确需要形象化表达，或抽象机制确实无法用动作和因果讲清楚时才考虑；900–1100 字口播最多一个，而且交付前必须再次判断能否删掉。没有真实素材时，不编造人物故事、客户经历或戏剧冲突。
- 每个段落不必都有结论金句。至少保留一处普通解释、一处停顿或不确定边界，让语气接近真实讲解。
- 少用连续反问、连续“你会发现”、连续“这说明”。把观众带到一个具体动作上，比替观众安排情绪更重要。
- 交付前检查：排比是否超过一组、比喻是否可删、是否出现三句以上同节奏短句、是否每段都在收束、是否有无法验证的故事细节、是否把“反直觉观点”等分析标签直接念给观众、是否把两层观点揉在一个段落里。命中就改写，不用为了保留字数继续堆句子。

## 口播连贯性硬约束

当本 Skill 参与生成或改写逐字稿时，必须额外检查段落之间的“为什么现在讲这个”，不能只检查单句是否顺口：

- Hook 抛出的角色、场景和问题，必须在正文开头 1–2 句内接住；不能 Hook 讲报价，正文突然从抽象定义开始。衔接要靠同一对象、同一动作或同一因果链继续推进，不要用“刚才老板问的”“回到刚才的场景”这类元话语提醒观众稿件结构。
- 公式、列表、案例、数字和练习每次切换时，都要补一句关系说明。承接可以继续同一个对象的动作、承接上一笔结果、提出下一步问题，或把抽象概念落回案例；示例句只是说明方向，不是固定话术。过渡句要说明前后关系，不是机械添加“首先、其次、接下来”。
- 新概念出现前，先说明它为什么能回答上一段的问题；新案例出现前，先说明它和前一个案例的关系；练习出现前，先说明观众要把刚才学到的什么做出来。
- 当内容有常规判断和新增判断时，先完整讲完常规判断，再用案例卡住或对比结果转入新增判断；新增判断至少另起一个段落。不要用“反直觉观点”“容易被忽略的信号”等结构标签代替真正的内容推进。
- 口播正文不反复解释创作过程、研究过程或“这是示例”。假设数字可以直接服务于推理；事实边界、来源说明和案例性质放在备注、研究依据或待补素材区域。
- 每个练习前必须有一段明确可听见的快速总结，先收束刚才的概念、判断顺序或案例结论，再说明练习要把哪一步做出来。可以使用“总结一下”“到这里先记住”“先收一下”等自然说法，但不要把它写成固定口号。
- 句子之间发生因果、转折、补充、条件或举例关系时，要把关系说出来。根据语气选择“所以、不过、接着、换个角度、这时、如果、除此之外”等自然连接词；前一句已经说完一个判断，后一句突然换成限制条件或价格条件时，必须补过渡。连接词服务理解，不要求每句都加，也不固定使用同一组词。
- 逐段复读时，检查观众能否回答三个问题：上一段刚讲了什么？这一段为什么出现？听完这一段下一步要做什么？答不上来就补关系句或调整顺序。

## 可直接录制的口播稿格式

当用户要口播稿，交付物必须是录制版，不是文章版：

- 过渡词要写进正文，让人直接念出来；不能只在分析里说“这里需要过渡”。
- 在关键位置加入少量非口播标注：`【停顿 0.3 秒】`、`【重读：结果】`、`【语气：放慢】`、`【转场】`。标注服务录制，不念出来。
- 每段按换气拆开，优先一句一行，用换行制造句间隔；同一段通常 1–3 个完整句子，长句拆成能一口气说完的短句。不要把完整句子机械拆成词组，也不要用连续大段文字承载多个判断。
- 标注只放在开场、观点切换、案例转场、练习和结尾等关键位置，不要每句都加，避免稿子变成操作说明书。
- 900–1100 字口播至少要有开场语气、2–4 处关键停顿、1–3 处转场或重读提示；字数统计以实际要念的文字为准，不把标注当成正文。
- Hook 之后、正文之前，可以加入一条简短的观看引导：说明接下来会解决什么问题或带走什么结果。已明确视频时长时，可以说“接下来用 3 分钟……”；没有明确时长，不擅自添加分钟数。引导要推进观看，不重复 Hook，也不做“保证学会”的空泛承诺。

## 外部能力边界

- 抖音视频下载和中文 SRT 由现有 `dylan-video-to-srt` 处理；本 skill 接收转写文本继续加工。
- 封面图片点击潜力由现有离线封面审计能力处理；本 skill 负责封面文字、标题承诺和内容匹配。
- 大量旧素材的内容工程化由另一个独立能力处理；单篇任务不要搭建重型知识库。

## 迁移说明

本目录是六个内容 skill 的完整本地迁移版，`modules/` 中保留了各模块的完整规则和公式库，并统一改为 `dylan-content` 的工作台入口。以后只修改本目录；不要重新安装或恢复已迁移的同名原版 skill。
